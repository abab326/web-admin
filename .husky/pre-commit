#!/bin/sh
LOG_FILE=".git/hooks/husky.log"
{
  echo "=== 开始执行: $(date) ==="
  echo "当前分支: $(git branch --show-current)"

  . "$(dirname "$0")/_/husky.sh"

  # 你的命令...
  npx lint-staged

  echo "=== 执行结束: $(date) ==="
  echo "退出码: $?"
} >> "$LOG_FILE" 2>&1
